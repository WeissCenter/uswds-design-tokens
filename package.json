{
  "name": "uswds-design-tokens",
  "version": "1.0.0",
  "description": "A lightweight Node.js program that processes USWDS design tokens from specified folders and compiles them into a JSON file compatible with Token Studio. It also converts the JSON file to CSS and JS variables using Style Dictionary and the Token Studio transform module.",
  "main": "compile.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tokens-studio/sd-transforms": "^0.11.6"
  }
}
